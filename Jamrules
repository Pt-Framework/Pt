#############################################################################
# General values and settings
#############################################################################

# include rules an actions to build unit tests
Load unittest.jam ;

# set directory to build objects and targets
ALL_LOCATE_OBJECTS = [ FDirName $(TOP) tmp $(CONFIG) ] ;
ALL_LOCATE_TARGET = [ FDirName $(TOP) build $(CONFIG) ] ;

# set directory to build objects and targets
# this is used by MakeLocate and SubDirObjects in the Jamfiles
OUTPUT_TARGET = [ FDirName $(TOP) build $(CONFIG) ] ;
OUTPUT_OBJECT = [ FDirName $(TOP) tmp $(CONFIG) ] ;
OUTPUT_TEST = [ FDirName $(TOP) build $(CONFIG) ] ;